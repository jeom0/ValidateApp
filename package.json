{
  "name": "querty-app",
  "version": "1.0.0",
  "description": "Hostinger Monorepo Root",
  "scripts": {
    "build": "cd server && npm install --ignore-scripts",
    "start": "node server/server.js"
  }
}
