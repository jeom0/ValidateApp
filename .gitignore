node_modules/
.DS_Store
*.log
.env

# Client specific
# client/dist/ is now allowed for production deployment

# Server specific
# server/data.db is now allowed for data persistence in production
server/server_log.txt
